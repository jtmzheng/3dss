package renderer.shader.types;

public enum ShaderTypes {
	RENDERING_SHADER,
	SKYBOX_SHADER,
	POST_PROCESS_SHADER,
	PICKING_SHADER,
	
	// Other (client doesn't need this shader identified)
	UNCLASSIFIED_SHADER
}
